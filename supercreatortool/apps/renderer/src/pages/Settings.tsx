import { useEffect, useState } from "react";
import { useSettingsStore } from "../store/settingsStore";

export default function Settings(){ const {settings,load,save}=useSettingsStore(); const [form,setForm]=useState<any>({}); useEffect(()=>{load();},[]); useEffect(()=>{if(settings) setForm(settings);},[settings]); return <div className='space-y-3'><h2 className='text-2xl font-bold'>Settings</h2><input className='bg-slate-900 border border-slate-800 p-2 rounded w-full' placeholder='Gemini API key' value={form.geminiApiKey||''} onChange={(e)=>setForm({...form,geminiApiKey:e.target.value})}/><input className='bg-slate-900 border border-slate-800 p-2 rounded w-full' placeholder='Output directory' value={form.outputDir||''} onChange={(e)=>setForm({...form,outputDir:e.target.value})}/><div className='grid grid-cols-2 gap-2'><input className='bg-slate-900 border border-slate-800 p-2 rounded' placeholder='Max concurrent' type='number' value={form.maxConcurrent||2} onChange={(e)=>setForm({...form,maxConcurrent:Number(e.target.value)})}/><input className='bg-slate-900 border border-slate-800 p-2 rounded' placeholder='Max retries' type='number' value={form.maxRetries||3} onChange={(e)=>setForm({...form,maxRetries:Number(e.target.value)})}/></div><button className='bg-violet-600 px-3 py-2 rounded' onClick={()=>save(form)}>Save Settings</button></div>; }
