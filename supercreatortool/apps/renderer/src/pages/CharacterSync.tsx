import { useState } from "react";
import { api } from "../lib/api";
export default function CharacterSync(){const [projectId,setProjectId]=useState('');const [characters,setCharacters]=useState([{name:'Hero',imagePath:''}]);const [prompts,setPrompts]=useState('{Hero} walking in rain');return <div className='space-y-3'><h2 className='text-2xl font-bold'>Character Sync</h2><input className='bg-slate-900 border border-slate-800 p-2 rounded' value={projectId} onChange={(e)=>setProjectId(e.target.value)} placeholder='Project ID'/>{characters.map((c,i)=><div key={i} className='grid grid-cols-2 gap-2'><input className='bg-slate-900 border border-slate-800 p-2 rounded' value={c.name} onChange={(e)=>setCharacters(characters.map((x,idx)=>idx===i?{...x,name:e.target.value}:x))}/><input className='bg-slate-900 border border-slate-800 p-2 rounded' value={c.imagePath} onChange={(e)=>setCharacters(characters.map((x,idx)=>idx===i?{...x,imagePath:e.target.value}:x))}/></div>)}<textarea className='w-full h-32 bg-slate-900 border border-slate-800 p-2 rounded' value={prompts} onChange={(e)=>setPrompts(e.target.value)}/><button className='bg-violet-600 px-3 py-2 rounded' onClick={()=>api.post('/character-sync',{projectId,characters,prompts:prompts.split('\n').filter(Boolean)})}>Start Character Video Generation</button></div>}
